using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerController : MonoBehaviour{
    // private bool hasControl = false; // player can interact with the game  
    // [SerializeField] private Grid tiles; 
    // [SerializeField] private Grid units; 
    // [SerializeField] private TileManager tileManager;
    // [SerializeField] private UnitManager unitManager;

    // void Awake(){
    //     GameManager.OnGameStateChanged += GameStateChanged;
    // }

    // void OnDestroy(){
    //     GameManager.OnGameStateChanged -= GameStateChanged;
    // }

    // private void Update(){
    //     if (_state == GameManager.GameState.PlayerTurn){

    //         if (Input.GetMouseButtonDown(0)){
    //             checkClick();
    //             Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition);
    //             RaycastHit hit; 

    //             if (Physics.Raycast(ray, out hit)){
    //                 GameObject unit = hit.collider.GameObject;

    //                 if (isChildOf(unit, units.transform){

    //                 }
    //             }
    //         }
    //     }
    // }

    // private bool isChildOf(GameObject object, Transform Grid){

    // }

    // private void GameStateChanged(GameManager.GameState _state){
       
    // }
}
